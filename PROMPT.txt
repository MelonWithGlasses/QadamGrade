**РОЛЬ:** Вы — старший Flutter-разработчик, специализирующийся на On-Device Machine Learning и API-интеграциях. Ваша задача — создать производственный код для мобильного приложения "QadamGrade" (или используйте то, что вы выбрали) с нуля.

**ТРЕБОВАНИЯ К КОДУ:**

* Используйте Dart и Flutter 3.x.
* Приложение должно быть одностраничным (main.dart) для простоты, но с четкой архитектурой, разделенной на сервисы.
* Все API-ключи должны быть константами-заглушками.

**ИНСТРУКЦИЯ (Выполнять строго по шагам):**

#### ШАГ 1: Настройка проекта и зависимостей (Dependencies)

Сначала создайте и выведите полный код файла **`pubspec.yaml`**. Обязательно включите следующие пакеты и их последние стабильные версии (если версия не найдена, используйте `any`):

1.  **Интерфейс и Камера:** `image_picker`, `flutter_markdown` (для красивого вывода ответа ИИ).
2.  **Сканирование (OCR):** `google_ml_kit_text_recognition`.
3.  **API:** `http`.
4.  **Локальное хранение (DB):** `hive`, `hive_flutter`, `path_provider`.

#### ШАГ 2: Создание сервиса сканирования (ImageScannerService)

Создайте класс **`lib/image_scanner_service.dart`**. Этот класс должен выполнять:

* Использовать `image_picker` для доступа к камере и получения объекта `XFile`.
* Использовать `TextRecognizer` из `google_ml_kit_text_recognition` для преобразования изображения в строку текста (`String`).
* Метод **`scanText()`** должен вернуть распознанный текст.
* Добавьте поддержку `TextRecognitionScript.cyrillic` (Кириллица, обязательно для русского и казахского языков).

#### ШАГ 3: Создание сервиса ИИ (OpenRouterService)

Создайте класс **`lib/openrouter_service.dart`**. Этот класс должен:

* Иметь константу `const String apiKey = 'SK_OR_V1_YOUR_NEW_KEY';`.
* Иметь метод **`evaluateHomework({required String taskText, required String studentAnswerText})`**.
* Метод должен отправлять **POST-запрос** на `https://openrouter.ai/api/v1/chat/completions`.
* **Обязательный Payload:** Установите `model` как `"google/gemma-3-27b-it:free"`.
* **Системный Промпт (System Prompt):** Используйте следующий текст, чтобы избежать галлюцинаций: "Ты — высококвалифицированный школьный учитель в Казахстане. Твоя задача: проанализировать ЗАДАНИЕ и ОТВЕТ УЧЕНИКА. Поставь приблизительную оценку по 10-балльной шкале. Затем, используя Markdown, объясни, где ошибка и что ученик должен улучшить в своем ответе. Отвечай доброжелательно на русском языке."
* Возвращаемое значение — строка с ответом ИИ.

#### ШАГ 4: Создание сервиса истории (LocalHistoryService)

Создайте класс **`lib/local_history_service.dart`**. Этот класс должен:

* Инициализировать Hive в методе `initHive()`.
* Определить модель данных `EvaluationResult` для хранения Задания, Ответа и Результата проверки.
* Метод **`saveResult()`** для сохранения данных в Hive.
* Метод **`getHistory()`** для загрузки всей истории проверок.

#### ШАГ 5: Интеграция, Главный экран и UI

Создайте финальный файл **`lib/main.dart`**. Он должен:

* Инициализировать все сервисы (`Hive`, `ImageScannerService`, `OpenRouterService`) перед запуском приложения.
* Создать StatefulWidget `HomeworkCheckerScreen`.
* **UI должен быть современным, интуитивно понятным и использовать Material Design 3.**

**Требования к структуре UI:**

1.  **Основной поток:** Интерфейс должен представлять собой вертикальный скроллируемый вид, четко разделенный на 3 этапа.
2.  **Шаги (Визуальное разделение):** Используйте **`Card`** виджеты для визуального отделения:
    * **Карточка 1:** **1️⃣ Сканировать Задание**. Крупная кнопка камеры. Статус отображает количество просканированных слов.
    * **Карточка 2:** **2️⃣ Сканировать Ответ Ученика**. Изначально **неактивна**, активируется после завершения Шага 1.
3.  **Кнопка Оценки:** Внизу экрана (или в виде `FloatingActionButton`) разместите кнопку **"ОЦЕНИТЬ ИИ"** (иконка 🤖), которая **активна** только после сканирования обоих текстов.
4.  **Результат:** После получения ответа ИИ, под карточками должен появиться:
    * Крупный, цветной **заголовок оценки** (например, **8/10**).
    * Раздел "Комментарий Учителя", использующий виджет **`MarkdownBody`** (из пакета `flutter_markdown`) для отображения структурированного текста от Gemma.
